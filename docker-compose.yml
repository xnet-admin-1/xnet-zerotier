services:
  xnet:
    build: .
    container_name: xnet
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    devices:
      - /dev/net/tun
    ports:
      - "9993:9993/tcp"
      - "9993:9993/udp"
      - "3001:3001/tcp"
    volumes:
      - ./data:/data
